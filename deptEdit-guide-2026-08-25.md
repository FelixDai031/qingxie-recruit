# 部门简介编辑 · 使用与上线说明（2026-08-25）

## 新交互流程
1. 管理员进「我的」→ 审核页。
2. 点卡片里的 **「进入编辑 ›」** 按钮。
3. 进入编辑页后，顶部 **下拉选择部门**。
4. 选完部门，下方出现「部门标语 / 部门简介」编辑框，改完点 **「保存修改」** 即可。
5. 部门详情页实时更新。

> 学生端看不到这个入口，只有管理员能进。

## 你「进不去」的 3 个根因（按概率）
1. **测的是手机搜索的线上版，但含编辑页的新版还没发布** → 线上没有 `deptEdit` 页，当然进不去。
2. **前端改完没重新编译/上传** → 真机预览扫的还是旧包，没有新页面。
3. **`adminCheck` 云函数没重部署** → 编辑页调 `listDepts`/`getDept` 失败，页面读不到部门。

## 立刻验证（最快，不用发布）
用 **开发者工具自带的模拟器**（电脑上）：
- 管理员账号登录 → 审核页 → 点「进入编辑 ›」→ 看能否进页面、能否下拉选部门、能否保存。
- 模拟器跑的是本地最新代码，**只要编译过就有 `deptEdit` 页**，能进就说明代码没问题，剩下只是发布流程。

## 上线必做（4 步）
1. `Ctrl+S` 保存全部文件 → 点「编译」（或重新点「预览」生成新二维码）。
2. 左侧云函数列表 → 右键 **`adminCheck`** → **「上传并部署：云端安装依赖」**（这次改了它，必须重部署）。
3. 右上角 **「上传」** 小程序代码（新增了 `deptEdit` 页面）。
4. mp.weixin.qq.com → 版本管理 → 提交审核 → 审核通过 **「发布」**。

## 云函数自检（不用手机）
右键 `adminCheck` →「测试」→ 测试参数填：
```json
{ "action": "listDepts" }
```
运行后返回 9 个部门数据 = 云函数 OK；报「未知操作」= 没部署新版本。

## 可选优化
把云开发控制台 `departments` 集合权限设为「所有用户可读，仅创建者可读写」，否则学生端（首页/部门详情）看不到管理员改后的最新简介。
