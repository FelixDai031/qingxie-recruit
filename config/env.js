// config/env.js —— 云开发环境配置
//
// 【首次使用必看】
// 把下面的 envId 改成「你自己的」云环境 ID，否则小程序连不上数据库。
//
// 获取方式：
//   微信开发者工具 → 云开发 → 环境概览 → 环境 ID
//
// 注意区分：
//   环境 ID   形如 cloud1-6giqy8k5d07e579b  ← 要填这个
//   环境名称  形如 "cloud1" 或你自己起的名字  ← 不是这个
//
// 为什么要单独抽出来：
//   云环境 ID 属于你的私有资源标识。仓库是公开的，写死在 app.js 里
//   一旦提交就会暴露，别人可以拿去读写你的云数据库。抽成单独文件后，
//   本地改真实值、仓库里只留占位符，两边互不干扰。

module.exports = {
  envId: 'YOUR_CLOUD_ENV_ID'
};
